	[JsonProperty(PropertyName = "#name#Id")]
    private readonly int #name#Id;
    public readonly #type# #name#
    {
        get
        {
		    if (DataManager.Instance.GetDataContainer<#type#>() == null)
                return default;
				
            return DataManager.Instance.GetDataById<#type#>(#name#Id);
        }
    }